package com.dvdfu.gems.handlers;

public class Enums {
	public enum Command {
		HOLD, BREAK, EXPLODE, DROWN, MOVE_UP, MOVE_DOWN, MOVE_RIGHT, MOVE_LEFT, PATH, FALL
	};
	
	public enum Cursors {
		ERASER, BLOCK_STATIC, BLOCK_ACTIVE, BLOCK_MOVE,
		BOMB, FALL, GEM_UP, GEM_DOWN, GEM_RIGHT, GEM_LEFT, GEM_CENTER,
		WATER, PATH, GATE
	};
}
